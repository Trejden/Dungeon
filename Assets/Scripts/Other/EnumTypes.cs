using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Assets.Scripts.Other
{
    public enum RoomType { NoRoom, Normal, Boss, Shop, Hidden, Locked, Treasure }
    public enum ConnectionType { NoConnection, Normal, Hidden, Locked }
    public enum Direction { Left, Up, Right, Down };
}
